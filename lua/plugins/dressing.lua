-- # Documentation for `dressing.nvim` Plugin
--
-- The `dressing.nvim` is a Neovim plugin that provides extensible core UI hooks, specifically `vim.ui.select` and `vim.ui.input`, introduced in Neovim 0.6. The plugin aims to improve upon the default behavior of these hooks.
--
--
return {
	"stevearc/dressing.nvim",
	event = "VeryLazy",
}
